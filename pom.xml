<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.sidev</groupId>
    <artifactId>couverture-code</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>quarkus</packaging>

    <properties>
        <compiler-plugin.version>3.15.0</compiler-plugin.version>
        <maven.compiler.release>25</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
        <quarkus.platform.group-id>io.quarkus.platform</quarkus.platform.group-id>
        <quarkus.platform.version>3.39.2</quarkus.platform.version>
        <skipITs>true</skipITs>
        <surefire-plugin.version>3.5.6</surefire-plugin.version>
        <quarkus-cucumber.version>1.3.0</quarkus-cucumber.version>
        <jacoco.version>0.8.14</jacoco.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>${quarkus.platform.artifact-id}</artifactId>
                <version>${quarkus.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-arc</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-rest</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-rest-jackson</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Cucumber : tests BDD (comportement) executes contre l'application Quarkus -->
        <dependency>
            <groupId>io.quarkiverse.cucumber</groupId>
            <artifactId>quarkus-cucumber</artifactId>
            <version>${quarkus-cucumber.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- Jacoco : collecte de la couverture pendant les tests Quarkus -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-jacoco</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <version>${quarkus.platform.version}</version>
                <extensions>true</extensions>
            </plugin>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${compiler-plugin.version}</version>
                <configuration>
                    <parameters>true</parameters>
                </configuration>
            </plugin>
            <!--
                Surefire est decoupe en deux executions distinctes (JUnit vs Cucumber) plutot
                que d'utiliser l'execution par defaut, pour que chacune s'attache a son propre
                fichier de couverture Jacoco (voir plugin jacoco-maven-plugin plus bas). C'est
                ce qui permet de "scinder" le rapport de couverture par type de test.
            -->
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire-plugin.version}</version>
                <configuration>
                    <systemPropertyVariables>
                        <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                        <maven.home>${maven.home}</maven.home>
                    </systemPropertyVariables>
                </configuration>
                <executions>
                    <!-- Desactive l'execution par defaut : remplacee par les deux ci-dessous. -->
                    <execution>
                        <id>default-test</id>
                        <phase>none</phase>
                    </execution>
                    <execution>
                        <id>tests-unitaires-junit</id>
                        <phase>test</phase>
                        <goals>
                            <goal>test</goal>
                        </goals>
                        <configuration>
                            <argLine>@{jacoco.argLine.junit}</argLine>
                            <excludes>
                                <exclude>**/cucumber/**</exclude>
                            </excludes>
                            <systemPropertyVariables>
                                <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                                <maven.home>${maven.home}</maven.home>
                                <!--
                                    L'extension quarkus-jacoco instrumente elle-meme, separement de
                                    jacoco-maven-plugin, le code qui tourne dans le QuarkusClassLoader
                                    (ex: un bean CDI appele depuis un @QuarkusTest). Sans ceci, ce code
                                    finirait dans target/jacoco-quarkus.exec (chemin par defaut de
                                    Quarkus, partage entre TOUTES les executions) au lieu du fichier
                                    dedie a cette execution -> on force un chemin par type de test.
                                -->
                                <quarkus.jacoco.data-file>${project.build.directory}/jacoco-junit-quarkus.exec</quarkus.jacoco.data-file>
                                <quarkus.jacoco.reuse-data-file>true</quarkus.jacoco.reuse-data-file>
                                <quarkus.jacoco.report>false</quarkus.jacoco.report>
                            </systemPropertyVariables>
                        </configuration>
                    </execution>
                    <execution>
                        <id>tests-integration-cucumber</id>
                        <phase>test</phase>
                        <goals>
                            <goal>test</goal>
                        </goals>
                        <configuration>
                            <argLine>@{jacoco.argLine.cucumber}</argLine>
                            <includes>
                                <include>**/cucumber/**</include>
                            </includes>
                            <systemPropertyVariables>
                                <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                                <maven.home>${maven.home}</maven.home>
                                <quarkus.jacoco.data-file>${project.build.directory}/jacoco-cucumber-quarkus.exec</quarkus.jacoco.data-file>
                                <quarkus.jacoco.reuse-data-file>true</quarkus.jacoco.reuse-data-file>
                                <quarkus.jacoco.report>false</quarkus.jacoco.report>
                            </systemPropertyVariables>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>${surefire-plugin.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <argLine>@{argLine}</argLine>
                    <systemPropertyVariables>
                        <native.image.path>${project.build.directory}/${project.build.finalName}-runner</native.image.path>
                        <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                        <maven.home>${maven.home}</maven.home>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <!--
                Jacoco : deux sources de donnees par execution (JUnit / Cucumber), a fusionner :

                1) L'agent jacoco-maven-plugin (-javaagent) attache par surefire, qui mesure
                   le code qui tourne HORS du QuarkusClassLoader (ex: ConflictDetectorTest qui
                   instancie ses classes directement, sans passer par le conteneur CDI).
                2) L'extension quarkus-jacoco (dependance test du pom), qui mesure separement
                   le code qui tourne DANS le QuarkusClassLoader (ex: un bean CDI appele depuis
                   un @QuarkusTest ou un scenario Cucumber) et ecrit dans le fichier pointe par
                   la propriete systeme quarkus.jacoco.data-file (voir plugin surefire ci-dessus,
                   un chemin distinct par execution).

                exclClassLoaders exclut precisement le QuarkusClassLoader de la mesure (1) pour
                eviter tout double comptage avec la mesure (2). Sans la fusion ci-dessous, la
                quasi-totalite du code applicatif (execute via CDI) resterait invisible dans les
                rapports par type de test.
            -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent-junit</id>
                        <phase>initialize</phase>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                        <configuration>
                            <exclClassLoaders>*QuarkusClassLoader</exclClassLoaders>
                            <destFile>${project.build.directory}/jacoco-junit.exec</destFile>
                            <propertyName>jacoco.argLine.junit</propertyName>
                        </configuration>
                    </execution>
                    <execution>
                        <id>prepare-agent-cucumber</id>
                        <phase>initialize</phase>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                        <configuration>
                            <exclClassLoaders>*QuarkusClassLoader</exclClassLoaders>
                            <destFile>${project.build.directory}/jacoco-cucumber.exec</destFile>
                            <propertyName>jacoco.argLine.cucumber</propertyName>
                        </configuration>
                    </execution>
                    <execution>
                        <id>merge-junit</id>
                        <phase>test</phase>
                        <goals>
                            <goal>merge</goal>
                        </goals>
                        <configuration>
                            <fileSets>
                                <fileSet>
                                    <directory>${project.build.directory}</directory>
                                    <includes>
                                        <include>jacoco-junit.exec</include>
                                        <include>jacoco-junit-quarkus.exec</include>
                                    </includes>
                                </fileSet>
                            </fileSets>
                            <destFile>${project.build.directory}/jacoco-junit-all.exec</destFile>
                        </configuration>
                    </execution>
                    <execution>
                        <id>merge-cucumber</id>
                        <phase>test</phase>
                        <goals>
                            <goal>merge</goal>
                        </goals>
                        <configuration>
                            <fileSets>
                                <fileSet>
                                    <directory>${project.build.directory}</directory>
                                    <includes>
                                        <include>jacoco-cucumber.exec</include>
                                        <include>jacoco-cucumber-quarkus.exec</include>
                                    </includes>
                                </fileSet>
                            </fileSets>
                            <destFile>${project.build.directory}/jacoco-cucumber-all.exec</destFile>
                        </configuration>
                    </execution>
                    <execution>
                        <id>merge-global</id>
                        <phase>test</phase>
                        <goals>
                            <goal>merge</goal>
                        </goals>
                        <configuration>
                            <fileSets>
                                <fileSet>
                                    <directory>${project.build.directory}</directory>
                                    <includes>
                                        <include>jacoco-junit.exec</include>
                                        <include>jacoco-junit-quarkus.exec</include>
                                        <include>jacoco-cucumber.exec</include>
                                        <include>jacoco-cucumber-quarkus.exec</include>
                                    </includes>
                                </fileSet>
                            </fileSets>
                            <destFile>${project.build.directory}/jacoco-merged.exec</destFile>
                        </configuration>
                    </execution>
                    <execution>
                        <id>report-junit</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <dataFile>${project.build.directory}/jacoco-junit-all.exec</dataFile>
                            <outputDirectory>${project.build.directory}/jacoco-report-junit</outputDirectory>
                        </configuration>
                    </execution>
                    <execution>
                        <id>report-cucumber</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <dataFile>${project.build.directory}/jacoco-cucumber-all.exec</dataFile>
                            <outputDirectory>${project.build.directory}/jacoco-report-cucumber</outputDirectory>
                        </configuration>
                    </execution>
                    <execution>
                        <id>report-global</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <dataFile>${project.build.directory}/jacoco-merged.exec</dataFile>
                            <outputDirectory>${project.build.directory}/jacoco-report</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>native</id>
            <activation>
                <property>
                    <name>native</name>
                </property>
            </activation>
            <properties>
                <quarkus.package.jar.enabled>false</quarkus.package.jar.enabled>
                <skipITs>false</skipITs>
                <quarkus.native.enabled>true</quarkus.native.enabled>
            </properties>
        </profile>
    </profiles>
</project>
